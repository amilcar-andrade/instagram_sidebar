<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorPrimaryDark">@android:color/white</item>
        <item name="colorAccent">@color/text_primary_dark</item>
    </style>

    <style name="MainActivityTheme" parent="@style/Theme.AppCompat.Light.NoActionBar"/>

    <style name="TextAppearance.Instagram" parent="TextAppearance.AppCompat.Subhead">
        <item name="android:textStyle">bold</item>

    </style>

</resources>
